# ICU 记录单提取系统依赖

# ── 图像处理 ──────────────────────────────────────────────────────────────────
opencv-python>=4.8
Pillow>=10.0

# ── OCR ──────────────────────────────────────────────────────────────────────
# PaddleOCR 需在独立 conda 环境中安装（见 cutter_worker*.py），
# 主环境只需以下轻量依赖：
# paddleocr>=2.7       # 若主环境也需要 OCR

# ── JSON 解析 ────────────────────────────────────────────────────────────────
json-repair>=0.25

# ── LLM 后端（选其一或两者都装）────────────────────────────────────────────────
# vLLM 客户端：通过 OpenAI 兼容 API 与 vLLM 服务通信
openai>=1.30

# Ollama 客户端（兼容旧版，可选）
ollama>=0.2

# ── Agent 编排 ───────────────────────────────────────────────────────────────
langgraph>=0.2

# ── PDF 转图片（可选，处理 PDF 输入时需要）──────────────────────────────────────
# 还需系统安装 poppler：
#   Ubuntu/Debian: sudo apt install poppler-utils
#   macOS:         brew install poppler
pdf2image>=1.17
